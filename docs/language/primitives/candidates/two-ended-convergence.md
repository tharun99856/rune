# Candidate: two-ended convergence (naming undecided, possibly = windowed-view)

**Status:** Under investigation. One data point, structurally ambiguous.

**Why is this being considered?**
Container With Most Water needs two positions converging from opposite ends
of a sequence — no current verb expresses this.

**Problems requiring it:** 017 (Container With Most Water) only.

**Open question:** is this the same shape as `windowed-view.md` (a window
that shrinks from both ends, rather than sliding or expanding from one), or a
genuinely distinct primitive? One data point can't settle this.

**Future work:** Deliberately attempt to express this problem as a
windowed-view variant before assuming it needs its own primitive.
